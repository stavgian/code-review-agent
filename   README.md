# Code Review Agent (Ollama + Node.js)

A local AI code reviewer powered by **Ollama**.  
It reviews your **staged git diff** and gives structured feedback.

## Features
- Local, private – no cloud needed
- Uses Ollama models (e.g. `llama3.1`)
- Structured review output
- Fast and simple CLI

## Installation

```bash
npm install
